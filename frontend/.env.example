# URL del backend visible desde el navegador (cliente Next.js).
# En produccion debe apuntar a la URL publica del API.
NEXT_PUBLIC_API_URL=http://localhost:8001

# URL del backend desde el server-side de Next (server actions, route handlers,
# server components). En Docker se resuelve por el nombre del servicio.
API_URL_INTERNAL=http://backend:8000

# Prefijo de URL bajo el cual se sirve la app (nginx, reverse proxy, etc.).
# Dejar vacio para servir en la raiz. Ejemplo: /portaltiltil
# Se usa como basePath y assetPrefix de Next.js.
NEXT_PUBLIC_BASE_PATH=
